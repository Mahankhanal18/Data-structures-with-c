// 2.Push, pop, display, peek and peep in linked list

#include <stdio.h>

int stack[100], choice, numberOfStackElements, top = -1, pushData, i;
void push(void);
void pop(void);
void display(void);
void peek(void);
void peep(void);

int main()
{
    printf("HEllo World ");
    return 0;
}